web: python manage.py migrate --noinput --skip-checks && { [ -n "$MEDIA_ROOT" ] && [ -d /app/media ] && [ "$MEDIA_ROOT" != /app/media ] && cp -rn /app/media/. "$MEDIA_ROOT"/ 2>/dev/null; true; } && gunicorn talisay_housing.wsgi:application --bind 0.0.0.0:${PORT:-8080} --workers 2 --threads 4 --timeout 180 --access-logfile - --error-logfile -
